/* ============================================================
   util.js — ฟังก์ชันช่วยเหลือที่ใช้ร่วมกันหลายไฟล์
   (โหลดก่อน combo.js / database.js / guides.js / roadmap.js เสมอ)
============================================================ */

window.escHtml = function escHtml(s) {
    return String(s == null ? "" : s).replace(/[&<>"']/g, c =>
        ({ "&": "&amp;", "<": "&lt;", ">": "&gt;", '"': "&quot;", "'": "&#39;" }[c]));
};
